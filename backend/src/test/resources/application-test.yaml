grpc:
  port: ${random.int(10000,40000)}

server:
  port: ${random.int(10000,40000)}

spring:
  main:
    web-application-type: reactive

  datasource:
    username: sa
    password:
    dbname: testdb
